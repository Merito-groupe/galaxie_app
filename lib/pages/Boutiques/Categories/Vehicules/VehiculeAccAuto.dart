import 'package:flutter/material.dart';

class VehiculeAccAutoScreen extends StatefulWidget {
  const VehiculeAccAutoScreen({super.key});

  @override
  State<VehiculeAccAutoScreen> createState() => _VehiculeAccAutoScreenState();
}

class _VehiculeAccAutoScreenState extends State<VehiculeAccAutoScreen> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}