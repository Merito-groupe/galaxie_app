import 'package:flutter/material.dart';

class MesPiecesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mes Pièces'),
      ),
      body: Center(
        child: Text('Contenu de la page Mes Pièces'),
      ),
    );
  }
}
