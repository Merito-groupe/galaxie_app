import 'package:flutter/material.dart';

class ChattezAvecNousPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chattez avec Nous'),
      ),
      body: Center(
        child: Text('Contenu de la page Chattez avec Nous'),
      ),
    );
  }
}
